'use client';

export function StandardMapAdminPage() {
  return (
    <div>
      <h1 style={{ fontSize: 22, fontWeight: 700, margin: '0 0 12px', color: '#1f2937' }}>普通地图管理</h1>
      <p style={{ fontSize: 14, color: '#6b7280', margin: 0 }}>
        普通地图后台入口已迁入 `standard-map` 包。后续在这里承载 POI、收藏、足迹等管理能力。
      </p>
    </div>
  );
}

