# 2026-05-16 17:36 - 审查mac简化交互：去toggle、详情改地图选择器

## 改动范围

- `src/app/(shell)/maps/page.tsx`
- `src/app/(shell)/maps/maps-page.module.css`
- `src/components/PlanMap.tsx`
- `docs/team-work/必看-协作规则.md`

## 改动说明

审查确认方向合理，核心变更：

1. **地图点击始终可用** — 移除 `pickModeOpen` 状态和 `mapPickMode` 属性，点击地图直接触发逆地理 POI 识别，不再需要先切模式
2. **工具栏简化** — 删除「地图选点」按钮，仅保留 `⋯` 详情按钮（36×32 小尺寸）
3. **详情弹窗改版** — 从搜索结果网格改为“全部地图种类”卡片选择器（普通地图 / 中国铁路），点击可切换 activeTab
4. **搜索图标** — 用 CSS 绘制的 `.searchGlyph` 放大镜替代 emoji 🔍
5. **PlanMap 清理** — 移除 `mapPickMode` prop、ref、click handler 内的判断

无冲突，改动清晰。
