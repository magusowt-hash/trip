# 本地映射模糊匹配长度限制

## 实现内容

- 映射本地时，模糊匹配只允许用于本地文件夹名长度大于等于 2 的目录。
- 本地文件夹名长度小于 2 时，不进入模糊匹配，只保留精准匹配结果。

## 实现方式

- 在本地目录匹配逻辑里，先取当前目录名并做空白裁剪。
- 仅当目录名长度大于等于 2 时，再执行现有模糊匹配判断。
- 长度小于 2 时直接跳过模糊匹配分支，避免单字目录误命中地点。
