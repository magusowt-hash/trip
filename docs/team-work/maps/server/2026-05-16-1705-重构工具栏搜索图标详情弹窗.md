# 2026-05-16 17:05 - 重构右侧面板工具栏、搜索图标、详情网格弹窗

## 改动范围

- `src/app/(shell)/maps/page.tsx`
- `src/app/(shell)/maps/maps-page.module.css`

## 改动说明

1. **删除「地点搜索」标题** — 右侧面板不再显示标题文字
2. **搜索框右侧加极简搜索图标** — 用 `🔍` 图标按钮替换原来的"搜索"文字按钮，放置在输入框内部右侧
3. **地图选点上移并减高** — 工具栏（地图选点 + 详情）放在搜索框上方，高度从 42px 减为 32px，视觉更紧凑
4. **横向按钮列表** — 工具栏用 `flex` 横向排列，地图选点在最左，详情在最右（spacer 推右）
5. **详情按钮 + 网格弹窗** — 点击「详情」弹出 modal，内为自适应网格布局（`auto-fill, minmax(200px, 1fr)`），展示所有搜索结果的名称、地址、收藏/足迹操作

## 验证方式

- 右侧面板顶部先看到工具栏（地图选点 | 详情）
- 搜索框内右侧有 🔍 图标，点击可触发搜索
- 点击「详情」弹出网格弹窗，显示全部地点卡片
- 弹窗内可直接执行收藏/足迹操作

## 注意事项

- 地图选点按钮改为 toggle（再次点击退出选点模式），`pickModeOpen` 状态切换
- 详情弹窗内收藏/足迹按钮文字缩短为「收藏」「足迹」以适配网格卡片宽度
